{
  "name": "job-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
